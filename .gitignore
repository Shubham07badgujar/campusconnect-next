# dependencies / build
node_modules
.next
out
dist
*.tsbuildinfo
next-env.d.ts

# secrets — never commit
.env
.env.*
!.env.example
service-account-key.json
rotated-credentials-*.csv

# logs / os
*.log
.DS_Store

# database backups — these hold student PII and, if any student has registered
# a face, biometric templates. Never commit them; see docs/BACKUPS.md.
backups/
*.backup.json
